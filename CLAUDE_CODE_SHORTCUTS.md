# Claude Code 快捷键与操作指南

## ⌨️ 基础快捷键

### 退出与中断
| 快捷键 | 功能 |
|--------|------|
| `ESC` x2 | 退出 Claude Code |
| `Ctrl + C` | 中断当前操作 |
| `Ctrl + D` | 退出 Claude Code（EOF） |

### 导航与查看
| 快捷键 | 功能 |
|--------|------|
| `Ctrl + R` | 查看完整对话记录（transcript mode） |
| `Ctrl + O` | 同上（旧版本） |
| `?` | 显示快捷键帮助 |

### 输入模式
| 快捷键 | 功能 |
|--------|------|
| `Tab` | 切换思考模式（thinking mode） |
| `Shift + Tab` | 切换自动接受模式（auto-accept） |
| `#` | 进入记忆输入模式 |
| `Ctrl + G` | 在系统编辑器中编辑提示词 |

### 历史与撤销
| 快捷键 | 功能 |
|--------|------|
| `↑` / `↓` | 浏览命令历史 |
| `Ctrl + Z` | 挂起 Claude Code（用 `fg` 恢复） |
| `Ctrl + \_` | 撤销提示词输入（undo） |

### 编辑
| 快捷键 | 功能 |
|--------|------|
| `Ctrl + Y` | 粘贴已删除的文本 |
| `Ctrl + U` | 清除当前行 |

## 🔄 重启 Claude Code

### 标准重启流程

```bash
# 1. 在 Claude Code 中
ESC ESC  # 快速按两次

# 2. 在终端中
claude  # 或使用启动脚本
```

### 快速重启命令

现在您有一个快速重启脚本：

```bash
bash ~/restart_claude.sh
```

这个脚本会：
1. 自动终止所有 Claude Code 进程
2. 等待进程完全退出
3. 重新启动 Claude Code

### 添加 Shell 别名（可选）

在 `~/.zshrc` 或 `~/.bashrc` 中添加：

```bash
# Claude Code 快捷命令
alias cc='claude'
alias ccr='bash ~/restart_claude.sh'  # 快速重启
alias ccc='cd "/Users/huan/Downloads/AI Learning/PM-AGI-Study" && claude'  # 切换目录并启动
```

然后运行：
```bash
source ~/.zshrc  # 或 source ~/.bashrc
```

使用：
```bash
ccr  # 快速重启 Claude Code
```

## 🎯 常用操作流程

### 1. 正常退出
```
在 Claude Code 中:
ESC ESC → 退出

在终端:
claude → 重启
```

### 2. 强制退出（卡住时）
```bash
# 新开终端，运行：
pkill -9 -f claude

# 然后重启
claude
```

### 3. 清理缓存后重启
```bash
# 退出 Claude Code (ESC ESC)
# 然后运行：
rm -rf ~/.claude/session-env/*
rm -rf ~/.claude/shell-snapshots/*
claude
```

### 4. 使用配置重启（验证 MCP）
```bash
# 退出后
claude

# 验证 MCP 是否加载
/mcp
```

## 📋 Claude Code 斜杠命令

在 Claude Code 内部使用（不是 shell 命令）：

| 命令 | 功能 |
|------|------|
| `/help` | 显示所有可用命令 |
| `/mcp` | 查看 MCP 服务器状态 |
| `/resume` | 查看和恢复历史会话 |
| `/status` | 查看当前状态和使用情况 |
| `/usage` | 查看详细使用统计 |
| `/model` | 切换模型 |
| `/clear` | 清除当前对话 |
| `/export` | 导出当前会话 |
| `/rewind` | 回退对话 |
| `/login` | 登录/切换账户 |
| `/logout` | 登出 |
| `/config` | 配置设置 |

## 🛠️ 故障排查快捷操作

### Claude Code 无响应
```bash
# 方式 1: 按 Ctrl+C 中断
# 方式 2: 按 ESC 两次退出
# 方式 3: 新终端运行
pkill claude && claude
```

### MCP 未加载
```bash
# 退出 → 检查配置 → 重启
cat ~/.claude/settings.json | grep -A5 "mcpServers"
claude
```

### 配置未生效
```bash
# 完全清理后重启
pkill claude
rm -rf ~/.claude/session-env/*
sleep 2
claude
```

## 💡 高效使用技巧

### 1. 快速切换项目
```bash
# 在 ~/.zshrc 中添加：
alias cc-pm='cd "/Users/huan/Downloads/AI Learning/PM-AGI-Study" && claude'
```

### 2. 带历史恢复启动
```bash
claude --continue  # 继续上次会话
claude --resume    # 选择历史会话
```

### 3. 调试模式启动
```bash
claude --debug  # 启用调试模式
```

## 🎯 当前您的快捷操作

### 验证 Tavily MCP
```bash
# 1. 退出
ESC ESC

# 2. 重启
claude

# 3. 验证
/mcp
```

### 快速重启（新）
```bash
bash ~/restart_claude.sh
```

---

现在您可以使用 `bash ~/restart_claude.sh` 快速重启 Claude Code！🚀

